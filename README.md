# 🚀 Express Starter Kit — Template

This repository contains the official **Express.js API template** used by the  
`@shubhamstr/boilerplate-generator`.

Although you can clone and use this template directly,  
**the recommended way is to generate a project using the CLI:**

```bash
npx @shubhamstr/boilerplate-generator my-api
